android {
            defaultConfig {
                applicationId = "com.datadirect.MusicMeta"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }